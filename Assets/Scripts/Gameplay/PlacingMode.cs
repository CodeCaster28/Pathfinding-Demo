namespace Gameplay
{
    /// <summary>
    /// Enum used to track in which placing mode grid painter is.
    /// </summary>
    public enum PlacingMode
    {
        Obstacle,
        StartPoint,
        GoalPoint
    }
}
